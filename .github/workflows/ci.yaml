name: CI

on:
  push:
    branches: ["master"]
  pull_request:

jobs:
  checks:
    name: x86_64-linux checks
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v7
      - uses: DeterminateSystems/determinate-nix-action@v3
      - run: nix flake check --print-build-logs
